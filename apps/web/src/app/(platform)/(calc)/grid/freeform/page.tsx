import { FreeformClient } from './FreeformClient';
export const metadata = { title: 'Malla de geometría libre (polígono) — GroundDesign Pro' };
export default function FreeformPage() { return <FreeformClient />; }
