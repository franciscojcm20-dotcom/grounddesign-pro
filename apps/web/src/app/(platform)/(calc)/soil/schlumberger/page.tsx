import { SchlumbergerClient } from './SchlumbergerClient';
export const metadata = { title: 'Resistividad Schlumberger — GroundDesign Pro' };
export default function SchlumbergerPage() { return <SchlumbergerClient />; }
