{
  "name": "grounddesing-pro-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "pnpm -r test",
    "test:legacy": "node --test tests/motors.test.js",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  }
}
